# =============================================================================
# agentmemory configuration
# =============================================================================
#
# Copy this file to `~/.agentmemory/.env` (or to your project root if you
# prefer scoped config) and uncomment the lines you want to override.
#
# Every line is OFF by default — `agentmemory` runs out of the box with no
# LLM key, no embedding key, and no API auth. Set keys here only when you
# want to enable the corresponding feature.
#
# Run `npx @agentmemory/agentmemory init` to copy this file into place
# automatically. Run `npx @agentmemory/agentmemory doctor` to verify that
# the daemon reads the env you expect.
#
# Defaults shown in comments. Listed in priority order — the first key
# present wins on the LLM detection path (see src/config.ts::detectProvider).

# -----------------------------------------------------------------------------
# 1. LLM provider — pick ONE
# -----------------------------------------------------------------------------
#
# Without a provider key, agentmemory runs in noop mode: observations are
# indexed via zero-LLM synthetic compression, hybrid search still works,
# but LLM-backed summarisation / reflection / consolidation are disabled.
# The detection order is OPENAI_API_KEY → MINIMAX_API_KEY → ANTHROPIC_API_KEY
# → GEMINI_API_KEY → OPENROUTER_API_KEY → noop.

# OPENAI_API_KEY=sk-...                          # Activates both the OpenAI-compatible LLM provider (DeepSeek, SiliconFlow, vLLM, LM Studio, Ollama via `/v1`) and OpenAI embeddings. Set OPENAI_API_KEY_FOR_LLM=false to scope it to embeddings only.
# OPENAI_BASE_URL=https://api.openai.com         # Override for OpenAI-compatible providers
# OPENAI_MODEL=gpt-5.6-luna                      # Default OpenAI-compatible chat model
# OPENAI_API_KEY_FOR_LLM=false                   # Skip OpenAI auto-detection for LLM; key stays active for embeddings

# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-5                # Default Anthropic model
# ANTHROPIC_BASE_URL=https://api.anthropic.com   # Override for Anthropic-compatible proxies / Azure AI Foundry
# ANTHROPIC_EFFORT=medium                        # low|medium|high|xhigh|max -> output_config.effort. Unset = the model's own
#                                                # default. Support is model-specific (GET /v1/models/{id} -> capabilities.effort):
#                                                # Opus 5 takes all five, Sonnet 4.6 all but xhigh, Haiku 4.5 rejects the parameter.

# GEMINI_API_KEY=...                             # Either env name works; GEMINI_API_KEY takes precedence
# GOOGLE_API_KEY=...                             # Alias for GEMINI_API_KEY when set alone (emits a one-time stderr hint)
# GEMINI_MODEL=gemini-3.7-flash                  # Default Gemini model (current stable Flash)

# OPENROUTER_API_KEY=sk-or-...
# OPENROUTER_MODEL=anthropic/claude-sonnet-5

# MINIMAX_API_KEY=...
# MINIMAX_MODEL=MiniMax-M3

# MAX_TOKENS=4096                                # Cap LLM completion tokens for compression / summarise calls

# Outbound LLM / embedding timeout — shared across every raw-fetch provider
# (Gemini, OpenRouter, MiniMax, OpenAI LLM, and OpenAI/Cohere/Voyage/OpenRouter
# embedding). The OpenAI LLM path also honours the OpenAI-scoped
# OPENAI_TIMEOUT_MS alias for back-compat with v0.9.17 (precedence).
# AGENTMEMORY_LLM_TIMEOUT_MS=60000                # Default: 60 000 ms (60 s)

# Opt-in Claude-subscription fallback (spawns @anthropic-ai/claude-agent-sdk
# child sessions). Off by default — the agent-sdk fallback can trigger
# Stop-hook recursion (#149 follow-up) when invoked from inside Claude Code.
# AGENTMEMORY_ALLOW_AGENT_SDK=true
# Most `claude` child sessions the agent-sdk fallback may run at once
# (default 2). Each child is ~190 MB resident and ignores SIGTERM when it
# wedges, so this is the bound on how much one provider outage can cost the
# host. Callers over the cap wait for a slot, then give up after
# AGENTMEMORY_AGENT_SDK_TIMEOUT_MS and fall back to synthetic compression.
# AGENTMEMORY_AGENT_SDK_MAX_CONCURRENCY=2
# How long one agent-sdk call may take before it is abandoned and the
# caller falls back to synthetic compression (default 180000 ms). Longer
# than AGENTMEMORY_LLM_TIMEOUT_MS because the budget covers spawning a
# `claude` CLI session on top of the model call. Falls back to
# AGENTMEMORY_LLM_TIMEOUT_MS when unset. Also bounds how long a caller
# waits for a concurrency slot.
# AGENTMEMORY_AGENT_SDK_TIMEOUT_MS=180000

# FALLBACK_PROVIDERS=anthropic,gemini            # Comma-separated chain tried after the primary provider returns an error (e.g. rate limit)

# -----------------------------------------------------------------------------
# 2. Embedding provider — auto-detected, override via EMBEDDING_PROVIDER
# -----------------------------------------------------------------------------
#
# Without an embedding key, agentmemory runs in BM25-only mode for hybrid
# search. Detection order: EMBEDDING_PROVIDER override → GEMINI_API_KEY →
# OPENAI_API_KEY → VOYAGE_API_KEY → COHERE_API_KEY → OPENROUTER_API_KEY →
# local (Xenova/all-MiniLM-L6-v2, 384-dim).

# EMBEDDING_PROVIDER=local                       # local | openai | voyage | cohere | gemini | openrouter

# VOYAGE_API_KEY=pa-...                          # Optimised for code embeddings

# COHERE_API_KEY=...                             # General-purpose embeddings

# Reuses OPENAI_API_KEY / OPENAI_BASE_URL above when EMBEDDING_PROVIDER=openai.
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # Embedding model when EMBEDDING_PROVIDER=openai
# OPENAI_EMBEDDING_DIMENSIONS=1536               # Required when the model is not in the known-models table

# OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small  # When EMBEDDING_PROVIDER=openrouter

# -----------------------------------------------------------------------------
# 3. Auth & security
# -----------------------------------------------------------------------------
#
# Bearer-token auth for the REST API + viewer + all integration plugins.
# Without a secret, REST endpoints are open on loopback. Set this when
# you expose the daemon beyond loopback or run behind a reverse proxy.

# AGENTMEMORY_SECRET=your-secret-here

# -----------------------------------------------------------------------------
# 4. Search tuning
# -----------------------------------------------------------------------------

# BM25_WEIGHT=0.4                                # Hybrid search weight for BM25 leg
# VECTOR_WEIGHT=0.6                              # Hybrid search weight for vector leg
# AGENTMEMORY_GRAPH_WEIGHT=0.2                   # Graph traversal bonus on smart-search ranking
# TOKEN_BUDGET=2000                              # Max tokens injected via mem::context per session
# MAX_OBS_PER_SESSION=500                        # Per-session observation cap before consolidation kicks in
# SUMMARIZE_CHUNK_SIZE=400                       # When mem::summarize sees a session larger than this, it chunks observations and map-reduces (chunk-summarize → reduce-merge) to stay within the LLM's context window. Default 400 ≈ 50k tokens per chunk at ~110 tok/obs. Native sessions are capped by MAX_OBS_PER_SESSION; chunking primarily matters for bulk-imported jsonl sessions, which bypass that cap.
# SUMMARIZE_CHUNK_CONCURRENCY=6                  # Parallel chunk LLM calls during chunked summarize. Default 6 fits ~100-chunk sessions under iii's 180s function-invocation timeout at typical ~8s/call. High-throughput providers (Novita, DeepInfra, DeepSeek) commonly allow 100+ concurrent — bump this for very large imported sessions.

# --- Retrieval ranking -------------------------------------------------
# Every flag below defaults to the historical behaviour. Turn them on one
# at a time and measure; several interact.

# AGENTMEMORY_FUSION=rrf                         # How the BM25 and vector legs are fused. 'rrf' (default) ranks on rank position alone — scale-free, but it discards relevance magnitude entirely, so a BM25 score of 74.9 and one of 42.2 differ only by where they landed. 'norm' fuses on normalised magnitude: BM25 min-maxed over the result set (it has no absolute scale), cosine mapped absolutely as (cos+1)/2 (min-maxing it would promote the best of a uniformly terrible vector set to 1.0).
# AGENTMEMORY_RRF_K=60                           # RRF rank constant. Larger flattens the gap between adjacent ranks further, smaller sharpens it. Ignored when AGENTMEMORY_FUSION=norm.
# AGENTMEMORY_MAX_PER_SESSION=3                  # Per-session result cap applied before the page is returned. Bounds how much one chatty session can dominate.
# AGENTMEMORY_DIVERSITY_EXEMPT_MEMORY_SESSION=true  # Exempt saved memories from the cap above. Every memory shares the synthetic sessionId "memory", so without this the ENTIRE memory corpus competes for AGENTMEMORY_MAX_PER_SESSION slots regardless of how many memories exist.
# AGENTMEMORY_MEMORY_LAYER_BOOST=1.25            # Multiplicative boost on the fused score for saved memories (id prefix mem_, or sessionId "memory"). Multiplicative so it needs no recalibration when the weights, RRF_K or the fusion mode move. Keep it a tiebreak, not an override — it is bounded only by the supersede de-index and the isLatest skip.
# AGENTMEMORY_RETRIEVAL_DEPTH=100                # Minimum candidates fetched from EACH leg before fusion, independent of the caller's page size. Legs were previously fetched at limit*2, which made results non-monotonic in limit: a document deep in the vector leg fell outside the fetch at small limits and forfeited its vector term, and under RRF that is unrecoverable (the vector term is worth up to 0.6/61 while the whole BM25 term tops out at 0.4/61). Curated memories are exactly the long, diffuse documents that sit deep in the vector leg. Nearly free — the vector scan is full regardless of limit; only the sort grows (~+8ms at 25k vectors). This is a CANDIDATE POOL size, not a page size - the caller still receives exactly `limit` rows. Set to 0 to disable the floor and restore the old limit*2 fetch.
# AGENTMEMORY_PREFIX_MATCH=max                   # How the prefix-expansion leg combines. 'max' (default) treats all index terms sharing a query-token prefix as ONE term and takes the best, bounding the contribution by construction. 'sum' restores the historical additive behaviour, in which each distinct sibling added its own full BM25 contribution — a document listing v1.0.0 through v1.40.0 accumulated 40 contributions for the single query token "v1" and outscored exact matches (measured 110.01 at 80 siblings against a ~22 single-term ceiling).
# AGENTMEMORY_INDEX_EXCLUDE_TOOL_PREFIXES=mcp__plugin_agentmemory_agentmemory__  # Comma-separated toolName prefixes whose observations are STORED but never INDEXED. Aimed at retrieval echoes — the daemon's own record of "the agent searched for X" contains the query verbatim, making it a near-perfect match for exactly the query that created it. The records stay fully readable via timeline, session history, the viewer and expandIds.

# --- Memory chunking ---------------------------------------------------
# AGENTMEMORY_MEMORY_CHUNKING=true               # Embed each saved memory as several vectors, one per ~section, instead of one vector for the whole thing. A long multi-topic memory otherwise averages out to a point near the corpus centroid and matches nothing. Also puts the memory's concepts into the embedded text, which the single-vector path never did. Requires a full index rebuild to take effect on existing memories.
# AGENTMEMORY_CHUNK_MAX_CHARS=1200               # Target chunk size, ~300 tokens (roughly one section). The binding constraint is semantic dilution, not model context.
# AGENTMEMORY_CHUNK_OVERLAP_CHARS=150            # Word-aligned overlap carried between adjacent chunks so a sentence spanning a boundary is still embedded intact somewhere. A value of 0 is treated as unset.

# AGENTMEMORY_INDEX_REBUILD=                     # Rebuild-once TOKEN (any string, not a boolean). At boot, if this differs from the token recorded in the store, the full index rebuild runs and the token is then recorded. Safe to leave set permanently — it will not rebuild twice for the same token.
# AGENTMEMORY_MEMORY_DOC_SLIM=true               # Drop the duplicated facts[0] from the BM25 document built for a memory (its content is otherwise indexed three times: title, narrative, facts). NOT a ranking win — measured, the fat document scores HIGHER, because duplicating content scales tf and docLen together and BM25 saturates in tf. Left off by default; enable only to shrink the index, not to improve recall.

# -----------------------------------------------------------------------------
# 5. Behaviour flags
# -----------------------------------------------------------------------------

# AGENTMEMORY_AUTO_COMPRESS=true                 # Run LLM compression on every observation batch (requires a provider key). Default off — synthetic compression handles most cases.
# AGENTMEMORY_INJECT_CONTEXT=true                # Inject recalled memories back into agent prompts (#143). Default off — hooks capture observations but do not modify conversation.
# CONSOLIDATION_ENABLED=true                     # Run the 4-tier consolidation pipeline (memories → semantic → procedural). Default off — opt in once you've measured the LLM cost.
# CONSOLIDATION_DECAY_DAYS=30                    # Age (days) after which non-reinforced memories decay during consolidation
# GRAPH_EXTRACTION_ENABLED=true                  # Extract concept-graph edges on remember; powers the graph-traversal recall path
# GRAPH_EXTRACTION_BATCH_SIZE=8                  # Memories per graph-extraction batch

# Local reasoning models only: set to 1 to ask the model to skip its
# hidden thinking pass during graph extraction. Extraction runs several
# times faster; relation quality can drop slightly. Leave unset to let
# the model think (default).
# AGENTMEMORY_LLM_NOTHINK=1

# AGENTMEMORY_REFLECT=true                       # Periodically auto-synthesize lessons from memories
# AGENTMEMORY_DROP_STALE_INDEX=true              # Drop on-disk BM25 / vector index on startup if dim guard fires (#248). Recovery toggle for stuck-state debugging.
# AGENTMEMORY_IMAGE_EMBEDDINGS=true              # Enable image embeddings when an image provider is present (experimental).

# -----------------------------------------------------------------------------
# 6. CLI / runtime knobs
# -----------------------------------------------------------------------------

# AGENTMEMORY_TOOLS=core                         # all (54 tools, default) | core (8 tools): surface exposed to MCP clients
# AGENTMEMORY_SLOTS=memory                       # Comma-separated plugin slot names the CLI should claim
# AGENTMEMORY_DEBUG=1                            # Trace MCP shim probe + standalone fallback decisions to stderr
# AGENTMEMORY_FORCE_PROXY=1                      # Skip the MCP shim livez probe and trust AGENTMEMORY_URL (for sandboxed MCP clients that can't reach localhost)
# AGENTMEMORY_PROBE_TIMEOUT_MS=2000              # MCP shim livez probe timeout
# AGENTMEMORY_URL=http://localhost:3111          # REST base URL — honored by status, doctor, MCP shim
# AGENTMEMORY_VIEWER_URL=http://localhost:3113   # Override the viewer URL printed by `agentmemory status`
# AGENTMEMORY_EXPORT_ROOT=~/agentmemory-backup   # Default destination for `agentmemory export`

# STANDALONE_MCP=1                               # MCP shim only — bypass the worker and run @agentmemory/mcp in-process
# STANDALONE_PERSIST_PATH=~/.agentmemory/local.db  # Path used by the standalone MCP shim's local fallback store

# Snapshot exporter — periodic snapshots of state_store + stream_store.
# SNAPSHOT_ENABLED=true
# SNAPSHOT_DIR=~/.agentmemory/snapshots
# SNAPSHOT_INTERVAL=3600                         # Seconds between snapshots

# Team sharing — when set, memories are scoped to (TEAM_ID, USER_ID) tuples.
# TEAM_MODE=shared
# TEAM_ID=acme
# USER_ID=rohit

# -----------------------------------------------------------------------------
# 7. Ports
# -----------------------------------------------------------------------------

# III_REST_PORT=3111                             # REST API port (also affects viewer at +2)
# III_STREAMS_PORT=3112                          # Streams API port
# III_ENGINE_URL=ws://localhost:49134            # iii-engine WebSocket URL (used by the worker)

# -----------------------------------------------------------------------------
# 8. iii engine pin
# -----------------------------------------------------------------------------
#
# agentmemory currently pins iii-engine to v0.11.2 — v0.11.6 introduces a
# new sandbox-everything-via-`iii worker add` model that agentmemory
# hasn't been refactored for yet. Override with AGENTMEMORY_III_VERSION
# only after migrating to the sandbox model manually.

# AGENTMEMORY_III_VERSION=0.11.2

# -----------------------------------------------------------------------------
# 9. Claude Code bridge (opt-in)
# -----------------------------------------------------------------------------

# CLAUDE_MEMORY_BRIDGE=true                      # Mirror compressed memories into Claude Code's CLAUDE.md
# CLAUDE_PROJECT_PATH=/path/to/your/project      # Required when CLAUDE_MEMORY_BRIDGE=true
# CLAUDE_MEMORY_LINE_BUDGET=200                  # Lines of memory CLAUDE.md should hold

# -----------------------------------------------------------------------------
# 10. Obsidian export (opt-in)
# -----------------------------------------------------------------------------

# OBSIDIAN_AUTO_EXPORT=true                      # Auto-export memories to an Obsidian vault on every consolidation

# -----------------------------------------------------------------------------
# 11. In-process engine (fork; iii engine retired)
# -----------------------------------------------------------------------------

# AGENTMEMORY_ENGINE=inproc                      # Run the daemon without the iii engine: state on node:sqlite, vectors as rows, BM25 rebuilt at boot
# AGENTMEMORY_SQLITE_PATH=/var/lib/agentmemory/agentmemory.sqlite  # The store (WAL mode); its directory must exist
# AGENTMEMORY_AUDIT_STORE=off                    # Emit audit entries as `[agentmemory] audit <operation> <json>` log lines instead of KV rows; memory_audit then answers an empty list
# AGENTMEMORY_AUDIT_LOG=all                      # With AUDIT_STORE=off: all | deletions (forget, delete, lesson_delete, slot_delete, core_remove, sketch_discard only) | off
# AGENTMEMORY_LOG_LEVEL=info                     # Lowest logger level written: info | warn | error | off. Audit lines bypass it.
