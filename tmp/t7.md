## Parent

#1

## What to build

The remaining call sites outside the functions directory — the HTTP and event trigger registration, the MCP server, the health monitor, and the process entry point — take their engine-facing types from this repository. Same mechanical migration as the functions batch, kept separate because these files carry the request-shape and worker-registration surface rather than plain function registration.

The entry point still registers the external worker at this stage; only its type imports move. Removing that registration is the contract ticket.

## Acceptance criteria

- [ ] No module outside the functions directory imports types from the external engine package, except the entry point's remaining worker registration.
- [ ] The HTTP request shape used by every endpoint comes from this repository and every endpoint still parses and responds identically.
- [ ] The full suite passes, with no test modified to accommodate the change.
- [ ] Every REST endpoint and MCP tool remains registered with the same path, method, and name as before.

## Blocked by

- #2
